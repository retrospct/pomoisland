<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Minimal hardened-runtime entitlements for an Electron app.
    Chromium/V8 require a writable+executable JIT region and load unsigned
    helper frameworks, so these three are mandatory for the app to launch
    under hardenedRuntime. PomoIsland only *plays* synthesized audio (Web
    Audio) and makes no network calls, so no microphone, camera, or network
    entitlements are requested.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
